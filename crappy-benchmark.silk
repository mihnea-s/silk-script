main;

use 'std/io';

fct factorial(x) {
  if (x == 1) {
    return 1;
  }
  return x * factorial(x - 1);
}

let start = now_millis();

for (let t = 1; t < 1000; t = t + 1) {
  factorial(t);
}

let total = now_millis() - start;

println('time taken: $total$ms');